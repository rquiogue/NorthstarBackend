# NorthstarBackend

Lightweight NestJS service scaffold.

## Endpoint

- `GET /api/v1/service`

Returns a mock list from a basic mock data layer (`src/data/mock.repository.ts`).

## Run

```bash
npm install
npm run dev
```
